import ReactECharts from 'echarts-for-react';

/**
 * interface for LiquidFillGauge series config
 */

export function LiquidFillChar(props): JSX.Element {
  return (
    <ReactECharts
      options={{
        Option: {
          series
        }
      }}
    />
  );
}
